[package]
name = "abc177"
version = "0.1.0"
authors = ["Kousuke Takaki <u7693@protonmail.com>"]
edition = "2018"

# dependencies added to new project
[dependencies]
proconio = "*"
petgraph = "*"

[profile.release]
lto = true
panic = 'abort'
